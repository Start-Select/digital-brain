// TODO: Phase 3 (Auth) — Login / Sign-up screen
// Email + password via Supabase Auth.
// On success, redirect to Today View ("/").

export default function LoginPage() {
  return (
    <div style={{ color: '#f5f5f5', padding: 24 }}>
      Login — coming in Phase 3
    </div>
  )
}
