// TODO: Phase 6 — Categories
// Create / rename / reorder / delete habit categories (name, color, emoji).

export default function CategoriesPage() {
  return (
    <div style={{ color: '#f5f5f5', padding: 24 }}>
      Categories — coming in Phase 6
    </div>
  )
}
